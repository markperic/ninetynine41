import { CategoryModulesPage } from "../category-page";

export default function StatsCategoryPage() {
  return (
    <CategoryModulesPage
      category="stats"
      title="Stats"
      description="Numbers-forward sections — starting with the two moved over from Social Proof."
    />
  );
}
