import { CategoryModulesPage } from "../category-page";

export default function NavCategoryPage() {
  return (
    <CategoryModulesPage
      category="nav"
      title="Nav"
      description="Site headers and floating navigation menus — a card-style expanding menu, a scroll-tracking chapter pill, and the kinetic overlay the site itself uses."
    />
  );
}
